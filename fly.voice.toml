app = 'kairo-voice-agent'
primary_region = 'iad'

[build]
  dockerfile = 'backend/Dockerfile.voice'

[processes]
  worker = 'python -m voice.kairo_voice_agent'

# No HTTP service — this is a background worker

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
